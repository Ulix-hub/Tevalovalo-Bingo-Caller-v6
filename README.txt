Tevalovalo Bingo Caller — v6 (Server-verified licensing, one-time per device)

Deploy (CLI quick):
1) npm i -g netlify-cli && netlify login
2) cd into this folder
3) netlify init
4) netlify env:set ADMIN_TOKEN "YourSuperLongSecret123!"
5) netlify deploy --prod

Generate codes: visit /owner.html on your live site, paste ADMIN_TOKEN, and click Generate.
Buyers unlock once per device, then can use offline.
